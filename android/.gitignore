# Signing material — NEVER commit. Anyone holding the keystore can publish an
# update that Android accepts as genuine. See README.md.
*.keystore
*.jks
keystore-password.txt

# Machine-specific SDK path
local.properties

# Generated by `node build-apk.js` and gradle
app/
build/
.gradle/
*.apk
*.idsig
*.log
